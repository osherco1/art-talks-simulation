---
description: 180-min take-home protocol (ARCHITECTURE.md is source of truth)
alwaysApply: true
---

# Prime Directive
You are the IDE Execution Model inside Cursor.
ARCHITECTURE.md is the single source of truth. Do not invent anything outside it.

# Zero-hallucination contract
- Do NOT create/assume new fields, endpoints, response shapes, screens, or flows that are not explicitly defined in ARCHITECTURE.md.
- If ARCHITECTURE.md is missing a required detail, STOP and ask for a focused clarification (1 question).

# Stepwise protocol: SCAN → PLAN → APPLY
- If the user prompt starts with "📋 SCAN": read/analyze only. NO file modifications.
- If it starts with "🧩 PLAN": design only. NO file modifications.
- If it starts with "🔧 APPLY": implement changes exactly as specified.

# Approval gate for code changes
- Only perform write operations when the prompt includes:
  "Approved. Please proceed."
- Otherwise: treat as SCAN/PLAN.

# Defensive coding requirements (mandatory)
Backend:
- No server crashes on invalid input or DB errors.
- Central error middleware.
- Stable JSON error format (as defined in ARCHITECTURE.md).
- Validate IDs and required fields.

Frontend:
- Every main screen must have loading / error / empty / success states.
- All HTTP calls live in a single ApiService.
- Handle server-down / timeouts gracefully (show UI error, do not crash).

# Scope discipline
- Only edit files inside the folders explicitly listed in the current prompt.
- Prefer minimal diffs. No refactors unless explicitly requested.

# Pre-injected files (must exist)
- ARCHITECTURE.md
- README.md
- backend/.env.example
- PROMPTS.md
- DECISIONS.md

# Repo skeleton (expected)
- backend/ (Node + Express + Mongoose)
- frontend/ (Flutter + GetX)
